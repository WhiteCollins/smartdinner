{
  "name": "smartdinner",
  "version": "1.0.0",
  "private": true,
  "description": "SmartDinner - Sistema Inteligente para la Gestión de Restaurantes",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
